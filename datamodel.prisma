type Product {
  id: ID! @unique
  name: String! 
  price: Float
  
  color: String!
  size: String!
  imagelink: String!
}

type launch {
  id: ID! @unique
  mission: String!
  details: String!
  Imagelink: String!
  price: Float!
}