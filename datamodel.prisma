type Query {
  products(id: ID!): Product
}

type Mutation {
  createProduct(name: String!, price: Float!, color: String!, size: String!): Product
  deleteProduct(id: ID!): Product
  updateProduct(id: ID!): Product
}

type Product {
  id: ID! @unique
  name: String!
  price: Float!
  color: String!
  size: String!
}









